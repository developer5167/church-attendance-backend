const express = require('express');
const router = express.Router();

// Add public endpoints here if needed in future
// Currently all volunteer endpoints require authentication

module.exports = router;
